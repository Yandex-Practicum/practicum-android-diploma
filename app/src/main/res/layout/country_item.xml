<?xml version="1.0" encoding="utf-8"?>
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/industry_region_button"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:background="?attr/colorOnSecondary">
        <TextView
            android:id="@+id/industry_country_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@id/industry_region_button"
            app:layout_constraintBottom_toBottomOf="@id/industry_region_button"
            android:text="@string/country_russia_text"
            android:paddingStart="@dimen/margin_16"
            android:fontFamily="@font/ys_display_regular"
            android:textSize="16sp"/>

        <ImageView
            android:id="@+id/ic_arrow_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_item_arrow"
            app:layout_constraintBottom_toBottomOf="@id/industry_country_text"
            app:layout_constraintEnd_toEndOf="@id/industry_region_button"
            app:layout_constraintTop_toTopOf="@id/industry_country_text"
            android:paddingEnd="24dp"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
